<div class="card mb-4">
    <img class="card-img-top"
         src="{{imgUrl}}"
         alt="Card image cap" width="400">
    <div class="card-body">
        <h4 class="card-title">{{title}}</h4>
        {{#if user}}
            <small class="float-right">Enrolled: {{enrolledUsers.length}}</small>
        {{/if}}
    </div>
</div>